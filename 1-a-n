#include <stdio.h>
#include <stdlib.h>

int main (int argc, char *argv[]) {
    int n;
    
    printf("Digite um número (n > 1): ");
    scanf("%d", &n);
    
    int cont = 1;
        while( cont <= n ){
        printf("%i\n", cont );
        cont = cont + 1;
   
    }
    
    return 0;
    
}
